<section class="hero-section-wrapper-1" style="background-color: #F3F3F3;">
  <!-- ========================= header-4 start ========================= -->
  <div class="header header-4" style="position: sticky; background-color: white;">
    <div class="navbar-area">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-lg-12 navbar navbar-expand-lg navbar-light" style="padding: 0; display: flex; justify-content: space-between; align-items: center;">
            <a class="navbar-brand" href="<%= root_path %>">
              <%= image_tag 'logo/logo.png', alt: 'Brand Logo' %>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent" style="flex-direction: row-reverse;">
              <ul class="navbar-nav">
                <% if current_user %>
                  <li class="nav-item">
                    <%= link_to "Profile", edit_profile_path(current_user), class: "page-scroll" %>
                  </li>
                  <% if current_user.admin? %>
                    <li class="nav-item">
                      <%= link_to "Admin Dashboard", admin_path, class: "page-scroll" %>
                    </li>
                    <li class="nav-item">
                      <%= link_to "Messages", contacts_path, class: "page-scroll" %>
                    </li>
                  <% end %>
                  <li class="nav-item">
                    <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "page-scroll" %>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to "S'inscrire", new_user_registration_path, class: "page-scroll" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Se connecter", new_user_session_path, class: "page-scroll" %>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
        <!-- row -->
      </div>
      <!-- container -->
    </div>
    <!-- navbar area -->
  </div>
  <!-- ========================= header-4 end ========================= -->
</section>
